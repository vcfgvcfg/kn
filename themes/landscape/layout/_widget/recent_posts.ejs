<% if (site.posts.length){ %>
    <ul>
    <% site.posts.sort('date', -1).limit(5).each(function(post){ %>
        <li>
        <a href="<%- url_for(post.path) %>"><%= post.title || '(no title)' %>-<%- url_for(post.category) %></a>
        </li>
    <% }) %>
    </ul>
<% } %>